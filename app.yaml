runtime: python39

# Production-grade resource allocation
instance_class: F2
automatic_scaling:
  min_instances: 1
  max_instances: 10
  min_idle_instances: 1
  max_idle_instances: 2
  min_pending_latency: 30ms
  max_pending_latency: 3s
  target_cpu_utilization: 0.6
  target_throughput_utilization: 0.6

# Environment variables with security considerations
# CRITICAL: Set JWT_SECRET_KEY via gcloud secrets manager in production
env_variables:
  # API Configuration
  API_V1_STR: "/api/v1"
  PROJECT_NAME: "Crow's Eye API - PostgreSQL"
  
  # PostgreSQL Database Configuration (Cloud SQL)
  DATABASE_URL: "postgresql+asyncpg://postgres:crowseye2024@/crowseye_db?host=/cloudsql/crows-eye-website:us-central1:crowseye-postgres"
  
  # JWT Configuration
  JWT_SECRET_KEY: "crow-eye-production-secret-key-change-this-in-production-2024"
  ALGORITHM: "HS256"
  ACCESS_TOKEN_EXPIRE_MINUTES: "10080"  # 7 days
  
  # Google Cloud Configuration
  GOOGLE_CLOUD_PROJECT: "crows-eye-website"
  GOOGLE_CLOUD_STORAGE_BUCKET: "crows-eye-storage"
  
  # API Keys (set these in Google Cloud Console)
  GOOGLE_API_KEY: "your-google-api-key"
  GEMINI_API_KEY: "your-gemini-api-key"
  OPENAI_API_KEY: "your-openai-api-key"
  
  # Database initialization flag
  INITIALIZE_DB: "true"
  
  # Security settings
  RATE_LIMIT_ENABLED: "true"
  SECURITY_HEADERS_ENABLED: "true"
  CORS_ORIGINS: "https://crow-eye-api-dot-crows-eye-website.uc.r.appspot.com,https://crows-eye-website.uc.r.appspot.com"
  
  # Logging configuration
  LOG_LEVEL: "INFO"
  STRUCTURED_LOGGING: "true"

# Health check configuration
readiness_check:
  path: "/health"
  check_interval_sec: 5
  timeout_sec: 4
  failure_threshold: 2
  success_threshold: 2
  app_start_timeout_sec: 300

liveness_check:
  path: "/health"
  check_interval_sec: 30
  timeout_sec: 4
  failure_threshold: 2
  success_threshold: 2
  initial_delay_sec: 300

# Network configuration
network:
  forwarded_ports:
    - 8080

# Security settings
entrypoint: gunicorn -k uvicorn.workers.UvicornWorker crow_eye_api.main:app

# Error handling
error_handlers:
  - file: error_pages/default_error.html
  - error_code: over_quota
    file: error_pages/over_quota.html

# Files are excluded via .gcloudignore for Python 3.9 runtime

# Resource allocation
resources:
  cpu: 1
  memory_gb: 1
  disk_size_gb: 10

# Cloud SQL instance connection
beta_settings:
  cloud_sql_instances: crows-eye-website:us-central1:crowseye-postgres

handlers:
- url: /.*
  script: crow_eye_api.main.application
  secure: always 
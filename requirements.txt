# Core dependencies
PySide6==6.9.0
requests==2.28.2
python-dateutil==2.8.2
python-dotenv==1.0.0
Pillow==11.2.1
google-generativeai==0.4.1
schedule==1.2.0

# Optional dependencies
PyPDF2==3.0.1
numpy==1.26.4
pytz==2023.3

# Simplified dependencies to avoid build issues
# Development dependencies can be installed separately if needed 